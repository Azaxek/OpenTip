# Environment for a DEMO deployment (see DEMO.md). Never use this for real tips.
DATABASE_URL=            # Neon pooled connection string
APP_SECRET=              # 32+ random characters
SETUP_TOKEN=             # you type this once at /demo to load the sample data
CRON_SECRET=             # lets Vercel Cron reset the demo nightly
DEMO_MODE=1
STORAGE_DRIVER=db
